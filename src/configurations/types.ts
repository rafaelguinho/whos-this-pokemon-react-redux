export interface LevelGenerations {
  level: number;
  generations: number[];
}

export interface RangeGenerations {
  generation: number;
  init: number;
  end: number;
}

export interface RangePokemons {
    init: number;
    end: number;
  }
